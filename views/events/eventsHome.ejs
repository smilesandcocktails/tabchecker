<div class="allEventsContainer">


<div class="eventHomeMainBtns">
  <form action="/events/add" method="GET">
    <button type="submit"><i class="material-icons">&#xE147;</i></button>
  </form>

  <form action="/logout" method="GET">
    <button type="submit"><i class="material-icons">&#xE8AC;</i></button>
  </form>
</div>

    <div class="eventList">
      <% events.forEach(function (each) { %>
        <div>
          <%= each.eventName %>
          on <%= formatDate(each.date)[0] %>
          @ <%= '$'+each.totalBill %>
          <form class="eventHomeBt" action=<%="/events/"+ each._id%> method="GET">
          <button type="submit"><i class="material-icons">&#xE417;</i></button>
          </form>
          <form class="eventHomeBt" method="POST" action="/events?_method=DELETE">
            <button value= <%=each._id%> name = "id" type = "submit"><i class="material-icons">&#xE92B;</i></button></td>
          </form>
        </div>
      <% }) %>
    </div>

</div>
